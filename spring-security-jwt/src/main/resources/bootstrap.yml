spring:
  cloud:
    nacos:
      password: nacos
      username: nacos
      discovery:
        server-addr: halo:8848
  application:
    name: halo-security
#  profiles:
#    active: test
server:
  port: 8090
